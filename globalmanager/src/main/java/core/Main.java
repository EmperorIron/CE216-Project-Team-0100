package core;

import gui.GUI;
import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        // Launch the JavaFX application from the gui package
           Application.launch(GUI.class, args);
    }
}